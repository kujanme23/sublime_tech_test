name: "KH_DIAG inbound has any link"
description: "Diagnostic: triggers on any inbound message containing at least one link."
type: rule
severity: low

source: |
  type.inbound
  and length(body.links) > 0
